#!/usr/bin/env node
/**
 * new-situation.mjs — scaffold situation seven.
 *
 *   npm run new:situation -- groundwater "India's groundwater" India
 *
 * WHAT THIS DOES, AND WHAT IT DELIBERATELY REFUSES TO DO.
 *
 * It writes a working generator with the FOUR SPINE BANDS from
 * SITUATION-SPEC.md §2.1 — top, strip, measured, act — wired to the shell, with
 * the ground chain already alternating and every write gate in place. That is
 * the part that is genuinely mechanical, and getting it right by default is
 * worth a couple of hours.
 *
 * It then prints the ELEVEN EVIDENCE TESTS from §2.2 with every optional band
 * marked UNANSWERED, and it does not write a single one of them.
 *
 * ★ THE REASON IT WILL NOT SCAFFOLD THE OPTIONAL BANDS.
 * The distribution in SITUATION-SPEC.md §3 is the argument: of the twenty bands
 * across six situations, FOUR are universal and FOURTEEN are used once or twice.
 * A scaffold that emitted all twenty slots would hand you eighteen empty ones,
 * and filling an empty band because the template has it is exactly what D-27
 * forbids — it is how the heatwave page ended up with a paragraph apologising
 * for having no money section.
 *
 * So the optional bands arrive as QUESTIONS, not as stubs. A band exists because
 * it has something to carry.
 *
 * ★ AND IT CANNOT ANSWER THE QUESTIONS. The two days that went into Air and
 * Yamuna were source verification over the network. No scaffold shortens that.
 */
import { writeFileSync, existsSync, readFileSync } from 'node:fs';
import { resolve, join } from 'node:path';

const ROOT = resolve(import.meta.dirname, '..');
const [id, title, where] = process.argv.slice(2);

if (!id || !title) {
  console.error(`Usage: npm run new:situation -- <id> "<Title>" [where]

  id      short slug, used for the filename and the FAMILY entry   e.g. groundwater
  Title   the h1, exactly as it should read                        e.g. "India's groundwater"
  where   Delhi | India                                            default: India

Read docs/design/SITUATION-SPEC.md first. Steps 1-3 there decide everything this
scaffold cannot.`);
  process.exit(1);
}
if (!/^[a-z][a-z0-9-]*$/.test(id)) {
  console.error(`id must be lowercase letters, digits and hyphens. Got "${id}".`);
  process.exit(1);
}

const genPath = join(ROOT, `scripts/build-situation-${id}.mjs`);
if (existsSync(genPath)) {
  console.error(`${genPath} already exists. Refusing to overwrite a generator.`);
  process.exit(1);
}

/* THE SPINE. Four bands, grounds already alternating so groundChain passes. */
const GEN = `// SITUATION SEVEN — ${title}. SCAFFOLDED, NOT FINISHED.
//
// Generated by scripts/new-situation.mjs. It contains the FOUR SPINE BANDS and
// nothing else, because SITUATION-SPEC.md §2.2's evidence tests have not been
// answered yet. Do not add a band until its test passes — a band exists because
// it has something to carry (D-27).
//
// BEFORE THIS PAGE CAN SHIP:
//   1. Replace every TODO below. Each one is a decision, not a placeholder.
//   2. The hero needs A NUMBER AND ITS PUBLISHED LIMIT. If no limit exists,
//      say "No legal threshold." in words — that is a valid answer (forest fire
//      is built on it). Inventing a benchmark is not.
//   3. Add this situation to FAMILY in scripts/lib/situation-shell.mjs. That one
//      edit gives it an index card, a crumb, and a place in six sibling rails.
//   4. Add it to the register in scripts/verify-final.mjs, then run
//      \`npm run verify:final\`.
import * as S from './lib/situation-shell.mjs';
const { esc, n0, n1, compact, opener, tabs, hole, kd, KIND_LEGEND, ARROW,
  stateChip, measureRow, measureHead, disclose, crumb, siblings } = S;

const sh = S.shell();

/* ═══ DATA ═══════════════════════════════════════════════════════════════
   TODO: read the committed dataset(s) for this subject. Never hardcode a
   figure here — the register in verify-final.mjs cross-checks the page against
   the same JSON, and a typed number defeats that.
   const D = S.J('${id}-india.json');
   ═══════════════════════════════════════════════════════════════════════ */

/* ═══ BAND SEQUENCE — id, tier class, ground hex ══════════════════════════
   No two adjacent grounds may match; groundChain checks it and assemble
   refuses to write on a clash. Add optional bands here ONLY as their evidence
   tests pass, keeping the alternation.
   ═══════════════════════════════════════════════════════════════════════ */
const BANDS = [
  ['top',      't1',       '#0D0D0B'],
  ['strip',    '',         '#151512'],
  ['measured', 'paper t2', '#F3F2F0'],
  ['act',      't3',       '#0D0D0B'],
];
const clashes = S.groundChain(BANDS);

const INDEX = [
  ['The reading', '#top'],
  ['How it is measured', '#measured'],
  ['What you can do', '#act'],
];

/* ═══ BANDS ══════════════════════════════════════════════════════════════ */
const B = {};

B.top = () => \`    <div class="pic ht">
      <img class="duo" src="/images/photos/TODO.jpg" alt="TODO: describe the photograph" style="--op:50% 50%">
      <div class="pic-over"><div class="wrap">
        <h1 class="d1">${title}</h1>
      </div></div>
    </div>
    <div class="pic-body"><div class="wrap p2-hero">
\${crumb('${id}')}
      <div class="p2-top">
        <p class="lbl p2-method">Every reading against its published limit. Every gap named.</p>
        <p style="margin:0"><span class="tag tag-season">Year round</span></p>
      </div>
      <div class="p2-cols">
      <div class="p2-read breach">
        <p class="state p2-state">\${stateChip('PERIODIC')}<span class="sr"> &mdash; TODO: why this state word</span></p>
        <p class="readout rl">TODO</p>
        <p \${kd('counted')}>TODO: the unit, and what the figure is of</p>
        <p class="verdict bad">TODO: the verdict, derived never typed</p>
        <p class="limit">TODO: the published limit and its authority. If none exists:
          <b>No legal threshold.</b> and say why in one sentence.</p>
        <p class="cap p2-src">TODO: source and observation time.
          <a class="lk" href="#measured">How this number is made</a>.</p>
      </div>
      <div class="p2-nat">
        <p class="lbl p2-nat-h">TODO: the one thing a reader should take from this</p>
        <p class="s7-plain">TODO: two short sentences. Numbers carry it, not adjectives.</p>
        <p style="margin:0"><a class="act" href="#measured">How it is measured \${ARROW}</a></p>
      </div>
      </div>
      <p style="margin:0"><a class="act" href="#measured">How it is measured \${ARROW}</a></p>
    </div></div>\`;

/* Four caged cells. This band ORIGINATES NOTHING — every cell points at the
   band that owns its figure. Red only where a published limit is broken. */
B.strip = () => {
  const cells = [
    ['TODO', 'TODO', 'TODO', 'measured', true],
    ['TODO', 'TODO', 'TODO', 'measured', true],
    ['TODO', 'TODO', 'TODO', 'measured', false],
    ['TODO', 'TODO', 'TODO', 'measured', false],
  ];
  return \`    <div class="wide p-strip-in">
      \${cells.map(([l, v, s, href, red]) => \`<a class="p-cell" href="#\${href}">
        <span class="p-cell-v\${red ? ' is-red' : ''}">\${v}</span>
        <span class="lbl p-cell-l">\${l}</span><span class="cap p-cell-s">\${s}</span></a>\`).join('\\n      ')}
      <p class="cap p-strip-note">One reading, one label. <a class="lk" href="#measured">What is behind them</a>.</p>
    </div>\`;
};

/* HOW IT IS MEASURED. On paper. If you cannot explain the derivation without
   hand-waving, do not publish the number. */
B.measured = () => \`\${opener('measured', 'How it is measured', 'TODO: one line on why the method matters more than the figure.')}
    <div class="wrap">
      \${tabs('How it is measured', [
        ['The rule', '<div class="s7-panel"><p>TODO: the threshold, its authority, and what it applies to.</p></div>'],
        ['The instrument', '<div class="s7-panel"><p>TODO: what measured it, at what cadence, and whether it is counted or modelled.</p></div>'],
        ['What this is not', '<div class="s7-panel"><p>TODO: the honest limits. This is usually the most valuable tab on the page.</p></div>'],
      ])}
      <p class="cap s7-src"><b>Sources.</b> TODO: name every one, with a link.</p>
      <p style="margin:0"><a class="act" href="#act">What you can do \${ARROW}</a></p>
    </div>\`;

/* WHAT YOU CAN DO. Every card must rest on something this page proved. */
B.act = () => \`\${opener('act', 'What you can do', 'TODO: one line.')}
    <div class="wrap">
      <div class="p-act">
        <div class="p-act-c">
          <p class="lbl">TODO</p>
          <p>TODO: an action built from a figure on this page.</p>
          <p style="margin:0"><a class="act" href="#top">TODO \${ARROW}</a></p>
        </div>
        <div class="p-act-c">
          <p class="lbl">TODO</p>
          <p>TODO.</p>
          <p style="margin:0"><a class="act" href="#measured">TODO \${ARROW}</a></p>
        </div>
        <div class="p-act-c">
          <p class="lbl">TODO</p>
          <p>TODO.</p>
          <p style="margin:0"><a class="act" href="/design/v3/home.html#give">Support the work \${ARROW}</a></p>
        </div>
      </div>
      <p class="cap s7-close">Every figure on this page is public, dated and linked.</p>
\${siblings('${id}')}
    </div>\`;

/* ═══ PAGE CSS — layout only. Nothing here may contain a template
   interpolation: this block is not extracted, but keeping the habit costs
   nothing and the shell's guard exists because it was broken once. ═══════ */
const PAGE_CSS = \`
.s7-plain{font-size:clamp(15px,1.05vw,17px);line-height:1.6;color:var(--fg-2);max-width:46ch;margin:0 0 .7em}
.s7-panel{max-width:64ch}
.s7-panel>p{font-size:clamp(14.5px,1vw,16.5px);line-height:1.58;color:var(--ink-2);margin:0 0 .85em}
.s7-src{max-width:62ch;color:var(--ink-3);margin:clamp(18px,2vw,26px) 0 clamp(20px,2.2vw,28px)}
.s7-close{max-width:60ch;color:var(--fg-3);margin:clamp(22px,2.4vw,32px) 0 0}
.is-red{color:var(--red)}
.paper .is-red{color:var(--red-ink)}
\`;

/* ═══ WRITE ══════════════════════════════════════════════════════════════ */
await S.assemble({
  file: 'situation-${id}.html',
  title: '${title.replace(/'/g, "\\'")} &mdash; Swechha',
  bands: BANDS, index: INDEX, sh, clashes,
  pageCss: PAGE_CSS,
  sectionFor: (bid) => (B[bid] || (() => '    <div class="wrap"><p class="lead">&mdash;</p></div>'))(),
  note: 'SCAFFOLD — 4 spine bands. Evidence tests unanswered; see SITUATION-SPEC.md §2.2.',
});
`;

writeFileSync(genPath, GEN);

/* ── THE EVIDENCE CHECKLIST. Questions, not stubs. ─────────────────────── */
const TESTS = [
  ['pan-India view', 'Is there a national dataset at the same cadence, or a station set you can compute one from?', '6/6'],
  ['hotspot / map', 'Do you have coordinates, or a named sequence you can order?', '6/6'],
  ['media register', 'Always available. Add one config row to scripts/refresh-coverage.mjs.', '6/6'],
  ['attention series', 'Is there an India-specific Wikipedia article with real traffic? A global article is worse than no band.', '5/6'],
  ['two disagreeing sources', 'Do two credible sources disagree? Publish both, never averaged.', '5/6'],
  ['health impact / deaths', 'Does a national table name your cause? NCRB Table 1.0 covers heat, flood, landslide, lightning, cyclone, forest fire.', '3/6'],
  ['money / budget', 'Is there a PRIMARY document with a figure you can attach? Not journalism reporting a panel.', '2/6'],
  ['localised, ward / pincode', 'Is there a per-location feed AND a server route to front it?', '1/6'],
  ['court records / orders', 'Do you have a court\'s OWN document? Cut twice before (D-11.1, D-19.1).', '0/6'],
  ['related campaigns', 'Is the campaign in SOURCE-FACTS.md, and is the connection in its own name?', 'index only'],
];

const w = Math.max(...TESTS.map(t => t[0].length));
console.log(`
Wrote scripts/build-situation-${id}.mjs — the FOUR SPINE BANDS, nothing else.

  npm run build:situation-${id}      # it builds now, full of TODOs

NEXT, IN THIS ORDER (SITUATION-SPEC.md §5):
  1. Find the LIMIT before the data. Who published a threshold, in what document?
  2. Verify every source over the network. Record the failures — a source is not
     available just because it is public.
  3. Answer the tests below. The band sequence falls out of the answers.
  4. Add '${id}' to FAMILY in scripts/lib/situation-shell.mjs.
  5. Add it to the register in scripts/verify-final.mjs, then verify:final.

THE ELEVEN EVIDENCE TESTS — all UNANSWERED. A band exists only if its test passes.
`);
for (const [name, q, n] of TESTS) {
  console.log(`  [ ] ${name.padEnd(w)}  ${n.padStart(10)}   ${q}`);
}
console.log(`
NOT SCAFFOLDED ON PURPOSE. Of the twenty bands across the six existing
situations, four are universal and fourteen are used once or twice. Emitting all
twenty would hand you eighteen empty slots, and filling a band because the
template has one is what D-27 forbids — it is how the heatwave page ended up
apologising for having no money section.

And nothing here answers the tests. The two days that went into Air and Yamuna
were source verification. No scaffold shortens that.
`);
